{
  "private": true,
  "name": "javascript",
  "author": "Umidbek Karimov <uma.karimov@gmail.com>",
  "repository": "https://github.com/dcodeteam/javascript.git",
  "engines": {
    "node": ">=10.0.0",
    "yarn": ">=1.10.0"
  },
  "scripts": {
    "preci-fix": "yarn install --force",
    "ci": "yarn lint && yarn cover -ci",
    "ci-fix": "yarn lint --fix && yarn cover",
    "lint": "stylotron lint",
    "cover": "yarn jest --coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^23.5.0",
    "lerna": "^3.10.5",
    "stylotron": "^0.0.12"
  }
}
