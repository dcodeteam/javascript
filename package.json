{
  "private": true,
  "name": "javascript",
  "author": "Umidbek Karimov <uma.karimov@gmail.com>",
  "repository": "https://github.com/dcodeteam/javascript.git",
  "engines": {
    "node": ">=8.3.0",
    "yarn": ">=1.10.0"
  },
  "scripts": {
    "pub": "lerna publish",
    "test": "yarn lint && yarn cover -ci",
    "lint": "cg lint",
    "cover": "yarn jest --coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "code-guard": "^0.1.0",
    "jest": "^24.8.0",
    "lerna": "^3.14.0"
  }
}
