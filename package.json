{
  "private": true,
  "name": "javascript",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "description": "JavaScript Style Guide",
  "author": "Umidbek Karimov <uma.karimov@gmail.com>",
  "repository": "https://github.com/dcodeteam/javascript.git",
  "engines": {
    "node": ">=8.0.0",
    "yarn": ">=1.10.0"
  },
  "scripts": {
    "preall": "yarn install --force",
    "ci": "yarn lint && yarn cover -ci",
    "all": "yarn lint --fix && yarn cover",
    "lint": "stylotron lint",
    "cover": "yarn workspaces run cover"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^23.5.0",
    "lerna": "^3.4.3",
    "stylotron": "^0.0.3"
  }
}
