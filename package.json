{
  "private": true,
  "name": "javascript",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "description": "JavaScript Style Guide",
  "author": "Umidbek Karimov <uma.karimov@gmail.com>",
  "repository": "https://github.com/dcodeteam/javascript.git",
  "jest": {
    "testEnvironment": "node"
  },
  "scripts": {
    "all": "yarn lint --fix && yarn test --no-watch",
    "test": "jest --watch",
    "lint": "js-scripts lint",
    "precommit": "lint-staged",
    "ci": "yarn lint && yarn test --ci --no-watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^23.5.0",
    "lerna": "^3.0.5"
  }
}
